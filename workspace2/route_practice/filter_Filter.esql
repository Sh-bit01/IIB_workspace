

CREATE FILTER MODULE filter_Filter
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		
		DECLARE var REFERENCE TO Root.XMLNSC;
		
		IF var.id = 10 THEN
			RETURN TRUE;
			
		ELSEIF var.id = 20 THEN
			RETURN FALSE;
			
		ELSE
			RETURN UNKNOWN;
		END IF;
	END;

END MODULE;
